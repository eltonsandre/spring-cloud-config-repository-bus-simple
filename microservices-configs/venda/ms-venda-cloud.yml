#SPRING_PROFILES_ACTIVE=application-default,application-de,default

config-app:
  msg: cloud -> Mensagem do service ${spring.application.name} ->  abacaxi

spring:
  profiles:
    include:
      - kafka-cloud
      - oracle
      - toggle
